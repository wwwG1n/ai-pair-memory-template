---
description: Bootstrap every Cursor session with shared-memory awareness and explicit skill routing.
globs:
alwaysApply: true
---

- Before substantive work, read `AGENTS.md`, `AI_CONTEXT.md`, `.ai-pair/status.json`, and `.ai-pair/current_handoff.md`.
- Treat `AI_CONTEXT.md` as long-lived memory and `.ai-pair/` as active workflow memory.
- Explicitly apply the logic of `using-superpowers` at the start of the conversation.
- If the shared-memory MCP server is available, prefer MCP tools for shared state updates.
- Do not assume Kimi has seen private Cursor tabs. Put durable state into shared memory.
