# OpenSpec Starter

This repository ships with a minimal OpenSpec layout:

- `spec/specs/` for living capability specs
- `spec/changes/` for active proposals

Use the OpenSpec gate before medium and large changes. The default capability spec in this template documents the shared-memory workflow itself.
